# apps/runtime/ledger — DEPRECATED

This directory is a renamed artifact from when the package was called `ledger`
before being renamed to `citadel`.

It contains no source files. The `__pycache__` entries here are stale compiled
bytecode and can be deleted safely.

**The canonical runtime package is at:** `apps/runtime/citadel/`

This directory will be removed once all stale bytecode is cleaned up.
